# 🎨 Desenho Visual: Sistema de Autenticação para Clientes

## 📊 Diagrama de Arquitetura

```
┌─────────────────────────────────────────────────────────────────────┐
│                         SISTEMA ATUAL                               │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│  Auth Users  │         │   Profiles   │         │   Clientes   │
│  (Supabase)  │◄────────┤              │         │              │
└──────────────┘         └──────────────┘         └──────────────┘
     │                         │                         │
     │                         │                         │
     │                         │                         │
  Usuários                  role:                    owner_id:
  Internos                  'superadmin'              (vinculado
  (superadmin,              'admin'                   ao usuário
   admin,                    'colaborador'             interno)
   colaborador)


┌─────────────────────────────────────────────────────────────────────┐
│              ACESSO APEXIA - SISTEMA ATUAL (MANTIDO)                │
└─────────────────────────────────────────────────────────────────────┘

    Cliente (qualquer um com link)
           │
           │ Acessa URL pública: /chat/:clientId/:sessionId
           │
           ▼
    ┌──────────────┐
    │ PublicClient │
    │     Chat     │
    └──────────────┘
           │
           │ Busca dados usando clientId da URL
           │
           ▼
    ┌──────────────┐
    │   Clientes   │
    │  (Banco DB)  │
    └──────────────┘

✅ MANTIDO: Qualquer um com link pode acessar (como está hoje)


┌─────────────────────────────────────────────────────────────────────┐
│                    SISTEMA PROPOSTO (NOVO)                          │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│  Auth Users  │         │   Profiles   │         │   Clientes   │
│  (Supabase)  │◄────────┤              │         │              │
└──────────────┘         └──────────────┘         └──────────────┘
     │                         │                         │
     │                         │                         │
     │                    role: 'cliente'            id (PK)
     │                    cliente_id: ───────────────────┘
     │                         │
     │                         │
Usuários                    Vinculação
Internos +                  direta entre
Clientes                    usuário e
                            cliente


┌─────────────────────────────────────────────────────────────────────┐
│              ACESSO APEXIA - SISTEMA NOVO (OPCIONAL)                │
└─────────────────────────────────────────────────────────────────────┘

    Cliente
           │
           │ 1. Acessa /login-cliente (OPCIONAL)
           │
           ▼
    ┌──────────────┐
    │ Client Login │
    └──────────────┘
           │
           │ 2. Email + Senha
           │
           ▼
    ┌──────────────┐
    │ Supabase Auth│
    │  (Autentica) │
    └──────────────┘
           │
           │ 3. Verifica profile.role = 'cliente'
           │    e profile.cliente_id
           │
           ▼
    ┌──────────────┐
    │   Protected  │
    │  Client Route│
    └──────────────┘
           │
           │ 4. Redireciona para /apexia
           │
           ▼
    ┌──────────────┐         ┌──────────────┐
    │   ApexIA     │────────►│   Clientes   │
    │ Authenticated│         │  (Banco DB)  │
    └──────────────┘         └──────────────┘
           │                         │
           │ Busca clientId do        │ RLS Policy garante
           │ profile (não da URL)     │ que só vê seus dados
           │                         │

🆕 NOVO: Acesso autenticado (cliente faz login)


┌─────────────────────────────────────────────────────────────────────┐
│                    FLUXO DE CRIAÇÃO DE LOGIN                        │
└─────────────────────────────────────────────────────────────────────┘

    Admin (você)
           │
           │ 1. Painel de Clientes
           │    Botão "Criar Login"
           │
           ▼
    ┌──────────────┐
    │  Formulário  │
    │ Email + Senha│
    └──────────────┘
           │
           │ 2. Submete
           │
           ▼
    ┌──────────────────────────────────────┐
    │   Supabase Admin API                 │
    │   1. Criar auth.users (email/senha)  │
    │   2. Criar profile:                  │
    │      - id = auth.users.id            │
    │      - role = 'cliente'              │
    │      - cliente_id = clientes.id      │
    └──────────────────────────────────────┘
           │
           │ 3. Credenciais criadas
           │
           ▼
    ┌──────────────┐         ┌──────────────┐
    │  Enviar Email│         │  Compartilhar│
    │  Automático  │    OU   │   Manual     │
    └──────────────┘         └──────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    ESTRUTURA DE DADOS                               │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ auth.users (Supabase - tabela gerenciada)                           │
├─────────────────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                                       │
│ email (text)                                                        │
│ encrypted_password (text)                                           │
│ created_at (timestamptz)                                            │
└─────────────────────────────────────────────────────────────────────┘
                            │
                            │ 1:1
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ profiles (tabela existente - MODIFICAR)                             │
├─────────────────────────────────────────────────────────────────────┤
│ id (uuid, PK) ────────────────────► FK para auth.users.id          │
│ role (text) ────────────► 'superadmin' | 'admin' |                 │
│                           'colaborador' | 'cliente' (NOVO)          │
│ full_name (text)                                                    │
│ avatar_url (text)                                                   │
│ cliente_id (uuid, NOVO) ────────────► FK para clientes.id          │
│                                    (NULL para usuários internos)    │
└─────────────────────────────────────────────────────────────────────┘
                            │
                            │ N:1 (muitos profiles podem ter
                            │     NULL cliente_id)
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ clientes (tabela existente - SEM MUDANÇAS)                          │
├─────────────────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                                       │
│ empresa (text)                                                      │
│ owner_id (uuid) ────────► FK para profiles.id (usuário interno)    │
│ ... (outros campos)                                                 │
└─────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                  EVOLUÇÃO FUTURA (Opcional)                         │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ cliente_user_access (tabela nova - FUTURO)                          │
├─────────────────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                                       │
│ cliente_id (uuid) ──────► FK para clientes.id                      │
│ user_id (uuid) ────────► FK para auth.users.id                     │
│ access_level (text) ───► 'apexia_only' | 'metrics' | 'full'        │
│ granted_modules (jsonb)► ['apexia', 'metrics', 'reports', ...]     │
│ created_at (timestamptz)                                            │
│ updated_at (timestamptz)                                            │
└─────────────────────────────────────────────────────────────────────┘

Uso futuro:
- Controle granular de acesso
- Múltiplos usuários por cliente
- Ativar/desativar módulos específicos


┌─────────────────────────────────────────────────────────────────────┐
│                      RLS POLICIES                                   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Policy: Clientes podem ver apenas seus dados                        │
├─────────────────────────────────────────────────────────────────────┤
│ SELECT * FROM clientes                                              │
│ WHERE id IN (                                                       │
│   SELECT cliente_id FROM profiles                                   │
│   WHERE id = auth.uid()                                             │
│   AND role = 'cliente'                                              │
│ )                                                                   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Policy: Clientes podem ver suas sessões de chat                     │
├─────────────────────────────────────────────────────────────────────┤
│ SELECT * FROM client_chat_sessions                                  │
│ WHERE client_id IN (                                                │
│   SELECT cliente_id FROM profiles                                   │
│   WHERE id = auth.uid()                                             │
│   AND role = 'cliente'                                              │
│ )                                                                   │
└─────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    ROTAS E NAVEGAÇÃO                                │
└─────────────────────────────────────────────────────────────────────┘

ROTAS PÚBLICAS:
┌─────────────────────────────────────────────────────────────────────┐
│ /login-cliente          │ ClientLogin.jsx (NOVO - opcional)          │
│ /login                  │ Login.jsx (usuários internos)             │
│ /chat/:clientId         │ PublicClientChat.jsx (MANTIDO - acesso    │
│ /chat/:clientId/:sessionId │ PublicClientChat.jsx  via link)        │
└─────────────────────────────────────────────────────────────────────┘

✅ Ambos os sistemas coexistem permanentemente!

ROTAS PROTEGIDAS - CLIENTES:
┌─────────────────────────────────────────────────────────────────────┐
│ /apexia                │ ApexIAAuthenticated.jsx (NOVO)             │
│ /apexia/:sessionId     │ ApexIAAuthenticated.jsx (NOVO)             │
└─────────────────────────────────────────────────────────────────────┘
           │
           │ ProtectedClientRoute verifica:
           │ - user autenticado
           │ - profile.role = 'cliente'
           │ - profile.cliente_id existe

ROTAS PROTEGIDAS - INTERNOS (existente):
┌─────────────────────────────────────────────────────────────────────┐
│ /dashboard, /clients, /projects, etc. (mantém como está)            │
└─────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                   COMPONENTES NECESSÁRIOS                           │
└─────────────────────────────────────────────────────────────────────┘

1. ClientLogin.jsx
   ├── Similar ao Login.jsx atual
   ├── Usa signInWithPassword do Supabase Auth
   └── Verifica role = 'cliente' após login

2. ProtectedClientRoute.jsx
   ├── Similar ao ProtectedRoute.jsx atual
   ├── Verifica profile.role = 'cliente'
   └── Redireciona para /login-cliente se não autorizado

3. ApexIAAuthenticated.jsx
   ├── Baseado em PublicClientChat.jsx
   ├── Busca clientId de profile.cliente_id (não da URL)
   └── Resto da lógica igual ao ApexIA atual

4. ClientUserManager.jsx (Admin)
   ├── Listar usuários vinculados ao cliente
   ├── Botão "Criar Login" para cliente
   ├── Formulário para criar usuário
   └── Opção para resetar senha


┌─────────────────────────────────────────────────────────────────────┐
│                    VANTAGENS DO DESIGN                              │
└─────────────────────────────────────────────────────────────────────┘

✅ FLEXIBILIDADE
   - Cliente escolhe: link público OU login autenticado
   - Links já compartilhados continuam funcionando
   - Não precisa forçar migração

✅ SEGURANÇA (Opcional)
   - Login autenticado usa Supabase Auth (robusto)
   - RLS policies garantem isolamento (acesso autenticado)
   - Cliente pode optar por mais segurança quando quiser

✅ REAPROVEITAMENTO
   - Usa infraestrutura existente (Supabase Auth)
   - Mantém estrutura de profiles
   - Fácil evoluir para controle granular

✅ UX
   - Link público: rápido e simples (como está hoje)
   - Login autenticado: opcional para quem quer mais segurança
   - Não precisa memorizar clientId na URL (se usar login)

✅ COMPATIBILIDADE
   - Mantém sistema atual funcionando
   - Mesmos dados compartilhados entre ambos
   - Transição suave (sem quebrar nada)


┌─────────────────────────────────────────────────────────────────────┐
│                    PRÓXIMOS PASSOS                                  │
└─────────────────────────────────────────────────────────────────────┘

FASE 1: Estrutura (1 dia)
□ Criar migration SQL (adicionar cliente_id em profiles)
□ Atualizar role em profiles existentes
□ Criar RLS policies (para acesso autenticado)

FASE 2: Autenticação (1 dia)
□ ClientLogin.jsx
□ ProtectedClientRoute.jsx
□ Atualizar App.jsx (adicionar novas rotas - manter antigas)

FASE 3: ApexIA Autenticado (1 dia)
□ ApexIAAuthenticated.jsx (baseado em PublicClientChat.jsx)
□ Adaptar para usar profile.cliente_id
□ **Manter PublicClientChat.jsx funcionando** (não alterar)

FASE 4: Admin (0.5 dia)
□ ClientUserManager.jsx
□ Integrar em Clients.jsx

FASE 5: Testes (0.5 dia)
□ Testar login cliente
□ Testar RLS
□ Testar acesso autenticado ao ApexIA
□ **Testar que acesso público continua funcionando**
□ Testar compartilhamento de dados entre ambos

TOTAL ESTIMADO: ~3-4 dias

⚠️ IMPORTANTE: Garantir que ambos os sistemas coexistem e compartilham dados!


┌─────────────────────────────────────────────────────────────────────┐
│                  RESUMO: COEXISTÊNCIA PERMANENTE                    │
└─────────────────────────────────────────────────────────────────────┘

✅ SISTEMA 1: ACESSO PÚBLICO (MANTIDO)
   Rota: /chat/:clientId/:sessionId
   Componente: PublicClientChat.jsx
   Status: ✅ Funciona como está hoje
   Uso: Link compartilhado (WhatsApp, Email)

✅ SISTEMA 2: ACESSO AUTENTICADO (NOVO)
   Rota: /apexia ou /apexia/:sessionId
   Componente: ApexIAAuthenticated.jsx
   Status: 🆕 Novo recurso (opcional)
   Uso: Login/senha para acesso seguro

🔄 COMPARTILHAMENTO
   Ambos compartilham:
   - Mesmas sessões de chat
   - Mesmas mensagens
   - Mesmos dados do cliente

💡 FLEXIBILIDADE
   Cliente escolhe:
   - Link rápido (público) OU
   - Login seguro (autenticado)
   
   Ambas as opções coexistem permanentemente!

