[Unit]
Description=Next.js DD CRM (ddcrm.jbapex.com.br)
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/jbapex/apps/crm
Environment=NODE_ENV=production
EnvironmentFile=-/root/jbapex/apps/crm/.env.local
ExecStart=/usr/bin/npm run start
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
